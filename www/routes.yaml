/:
  controller: Main
  action: home

/config:
  controller : Main
  action : config

/login:
  controller: Security
  action: login

/logout:
  controller: Security
  action: logout

/register:
  controller: Security
  action: register